services:
  nut-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nut-monitor-web
    ports:
      - "3000:3000"
    environment:
      - UPS_NAME=ups          
      - UPS_HOST=192.168.1.101
    restart: unless-stopped